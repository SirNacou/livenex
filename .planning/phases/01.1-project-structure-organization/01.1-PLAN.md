---
phase: 01.1-project-structure-organization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - package.json
  - tsconfig.json
  - src/
  - src/db/schema.ts
  - src/lib/
  - src/api/
  - src/types/
  - app.config.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Project directory structure follows TanStack Start conventions with clear frontend/backend/shared separation"
    - "Database schema foundation exists with core patterns (timestamps, primary keys, soft deletes)"
    - "API route structure established with consistent response envelope pattern"
    - "Naming conventions documented and applied to database, files, and code"
    - "Error handling patterns defined for authentication, validation, and server errors"
  artifacts:
    - path: "package.json"
      provides: "Project metadata and dependency declarations"
      contains: ["tanstack/react-start", "elysia", "drizzle-orm", "better-auth", "zod", "tailwindcss"]
    - path: "src/app.ts"
      provides: "ElysiaJS application entry point with middleware, error handlers, response envelope"
      exports: ["Elysia instance", "error middleware", "response envelope"]
    - path: "src/lib/db.ts"
      provides: "Drizzle ORM database instance and connection"
      exports: ["db instance"]
    - path: "src/types/index.ts"
      provides: "Shared type definitions and response envelopes"
      exports: ["ApiResponse<T>", "ApiError", "AuthContext"]
    - path: "src/db/schema.ts"
      provides: "Core database schema with users, sessions, api_keys tables"
      contains: ["model User", "model Session", "model ApiKey", "timestamps pattern", "soft delete pattern"]
  key_links:
    - from: "src/app.ts"
      to: "src/lib/db.ts"
      via: "instantiate database connection on startup"
      pattern: "const db = new Drizzle"
    - from: "src/api/*"
      to: "src/types/index.ts"
      via: "import and use ApiResponse<T> in all endpoints"
      pattern: "import.*ApiResponse.*from.*types"
    - from: "app.config.ts"
      to: "package.json"
      via: "TanStack Start config references installed dependencies"
      pattern: "config.*entry"
---

<objective>
Establish the architectural foundation for Livenex by creating a standard project structure, database schema patterns, API route organization, naming conventions, and error handling that downstream phases will build upon.

Purpose: Phase 1.1 work happens before Phase 1 implementation so that Phase 1 (private access) and all downstream phases (Phase 2-5) can follow consistent structural patterns without mid-flight refactoring.

Output: Initialized project with directory layout, core database patterns, API structure, and documented conventions ready for Phase 1 implementation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@.planning/phases/01.1-project-structure-organization/01.1-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize project structure and package.json</name>
  <files>
    package.json
    tsconfig.json
    app.config.ts
    src/
    .gitignore
  </files>
  <action>
Create the core project structure following TanStack Start conventions with clear frontend/backend separation:

1. **Directory Structure** (per TanStack Start + ElysiaJS patterns):
   src/
   ├── app.ts                    # ElysiaJS app entry point
   ├── client/                   # TanStack Start client-side (pages, routes, components)
   │   ├── routes/               # File-based routing (TanStack Start)
   │   └── components/           # React components
   ├── server/                   # Server-side handlers and middleware
   │   ├── auth/                 # Authentication flows (better-auth setup)
   │   └── middleware/           # Request middleware
   ├── api/                      # ElysiaJS API routes (organized by domain)
   │   ├── auth.ts               # Auth endpoints (/api/auth/*)
   │   └── health.ts             # Health check endpoints
   ├── lib/                      # Shared utility libraries
   │   ├── db.ts                 # Drizzle ORM setup
   │   ├── errors.ts             # Error classes and handling
   │   └── validation.ts         # Zod schema validation helpers
   ├── types/                    # Shared TypeScript type definitions
   │   ├── index.ts              # Core response envelopes, ApiResponse<T>, ApiError
   │   └── auth.ts               # Auth-related types (User, Session, ApiKey)
   ├── db/                       # Database layer (Drizzle ORM)
   │   ├── schema.ts             # Database schema definitions
   │   └── migrations/           # SQL migrations (generated by drizzle-kit)
   ├── constants/                # Application constants
   └── config.ts                 # Runtime configuration

2. **package.json** — Create with dependencies from STACK research:
   - Core: @tanstack/react-start, react, react-dom, elysia, drizzle-orm, pg
   - Auth: better-auth
   - Validation: zod
   - Styling: tailwindcss, postcss, autoprefixer
   - Database: drizzle-kit, @types/pg
   - Dev: typescript, tsx, @biomejs/biome, vitest, @playwright/test
   - Keep versions aligned with STACK.md research
   - Set "type": "module" for ES modules

3. **tsconfig.json** — Configure for strict TypeScript with:
   - compilerOptions: target ES2020, module esnext, strict: true
   - paths: @/* → src/*
   - jsx: react-jsx for React 19
   - skipLibCheck: true, esModuleInterop: false

4. **app.config.ts** — TanStack Start configuration

5. **.gitignore** — Add node_modules, dist, .env.local, build, database files

Per D-01 (agent's discretion on directory layout), follow TanStack Start + ElysiaJS best practices.
  </action>
  <verify>npm install && npm run build 2>&1 | grep -E "(error|Error)" || echo "Build succeeded"</verify>
  <done>
    - package.json exists with all core and dev dependencies
    - Directory structure created with frontend/backend/shared separation
    - tsconfig.json configured for strict TypeScript
    - app.config.ts defines TanStack Start entry points
    - .gitignore excludes build artifacts and secrets
    - npm install completes without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create database schema foundation with core patterns</name>
  <files>
    src/db/schema.ts
    src/lib/db.ts
    drizzle.config.ts
    .env.example
  </files>
  <action>
Establish the database foundation with Drizzle ORM schema patterns that Phase 2+ will extend:

1. **src/db/schema.ts** — Define core tables with Drizzle ORM using UUID primary keys, timestamps, and soft-delete patterns

2. **src/lib/db.ts** — Initialize Drizzle ORM connection to PostgreSQL

3. **drizzle.config.ts** — Configure migrations for PostgreSQL driver

4. **.env.example** — Template environment variables (DATABASE_URL, REDIS_URL, etc.)

Per D-02, D-03: Use UUID primary keys, explicit timestamps (created_at/updated_at), and deleted_at soft-delete column.

Schema includes: users, sessions, api_keys, audit_logs tables only (monitoring entities deferred to Phase 2).
  </action>
  <verify>npm run drizzle:generate 2>&1 && ls src/db/migrations/*.sql 2>/dev/null | wc -l</verify>
  <done>
    - src/db/schema.ts defines core tables with Drizzle ORM
    - All tables use UUID primary keys
    - All tables include created_at and updated_at timestamps
    - Relationships defined with foreign key constraints
    - src/lib/db.ts exports initialized Drizzle instance
    - drizzle.config.ts configured for PostgreSQL migrations
    - Initial migration generated
    - .env.example includes all required environment variables
  </done>
</task>

<task type="auto">
  <name>Task 3: Establish API route structure and response envelope patterns</name>
  <files>
    src/app.ts
    src/types/index.ts
    src/lib/errors.ts
    src/api/health.ts
    src/api/auth.ts
  </files>
  <action>
Create the ElysiaJS a
